# Disable Structured Output (clean provider toggles)

Removes response_format/response_schema/response_mime_type and schema
tool if present. Keeps user tools intact.

## Usage

``` r
disable_structured_output(config)
```

## Arguments

- config:

  llm_config
