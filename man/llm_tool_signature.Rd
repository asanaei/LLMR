% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tools.R
\name{llm_tool_signature}
\alias{llm_tool_signature}
\title{A stable signature for a tool's identity}
\usage{
llm_tool_signature(tool)
}
\arguments{
\item{tool}{An \code{llmr_tool} (from \code{\link[=llm_tool]{llm_tool()}}), or a list carrying \code{name},
\code{description}, and \code{schema}.}
}
\value{
A 64-character lowercase hex string.
}
\description{
Returns a SHA-256 hash over a tool's \code{name}, \code{description}, and parameter
\code{schema} (its implementation function is deliberately excluded, so the
signature tracks the contract a model sees, not the R code). Use it to detect
when a tool's advertised interface has changed, for example to pin a tool
schema and notice a silent redefinition.
}
\examples{
tl <- llm_tool(function(x) x, "echo", "Echo a value",
               parameters = list(x = list(type = "string")))
llm_tool_signature(tl)
}
\seealso{
\code{\link[=llm_tool]{llm_tool()}}, \code{\link[=llm_hash]{llm_hash()}}.
}
