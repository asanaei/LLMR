% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usage.R
\name{llm_failures}
\alias{llm_failures}
\title{List the rows of an LLM run that failed or were truncated}
\usage{
llm_failures(x, prefix = NULL, include = c("all", "failed", "truncated"))
}
\arguments{
\item{x}{A data frame from \code{\link[=call_llm_par]{call_llm_par()}} or \code{\link[=llm_mutate]{llm_mutate()}}.}

\item{prefix}{For an \code{\link[=llm_mutate]{llm_mutate()}} result, the output column name whose
diagnostics to summarize (e.g. \code{"answer"}). Inferred automatically when a
single diagnostic block is present; required when several are.}

\item{include}{One of \code{"failed"} (hard failures only), \code{"truncated"}
(length/filter only), or \code{"all"} (default; both).}
}
\value{
A tibble (zero rows if nothing matched) with: \code{row} (index into \code{x}),
\code{success}, \code{finish_reason}, \code{status_code}, \code{error_code}, \code{bad_param},
\code{error_message}, \code{response_id}. Columns absent from \code{x} are filled with
\code{NA}.
}
\description{
Returns one row per problem: a hard failure (\code{success} not \code{TRUE}) or a
truncated / content-filtered completion (\code{finish_reason} \code{"length"} or
\code{"filter"}), with the diagnostic detail needed to act. Works on both
\code{\link[=call_llm_par]{call_llm_par()}} and \code{\link[=llm_mutate]{llm_mutate()}} results. For a \code{\link[=call_llm_par]{call_llm_par()}} result
(which still carries \code{config} and \code{messages}), pass the original frame to
\code{\link[=llm_par_resume]{llm_par_resume()}} to re-run only these rows.
}
\examples{
res <- tibble::tibble(
  success = c(TRUE, FALSE, TRUE),
  finish_reason = c("stop", "error:server", "length"),
  error_message = c(NA, "HTTP 503", NA)
)
llm_failures(res)
}
\seealso{
\code{\link[=llm_par_resume]{llm_par_resume()}} to re-run failed rows, \code{\link[=llm_usage]{llm_usage()}}.
}
