% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/personas.R
\name{llm_persona_split}
\alias{llm_persona_split}
\title{Split one persona into labeled demographics and labeled answers}
\usage{
llm_persona_split(x, i, drop = "ideology_score")
}
\arguments{
\item{x}{A persona data frame (see \link{anes_2024_personas}).}

\item{i}{Integer row index.}

\item{drop}{Character vector of column names to exclude from both parts
(defaults to \code{"ideology_score"}).}
}
\value{
A list with two named character vectors, \code{demographics} and
\code{responses}.
}
\description{
Extracts row \code{i} of a persona frame into two named character vectors: the
demographic fields and the survey/attitude answers. Names are the question
wording when the frame carries a \code{dictionary}, otherwise the column handles.
Missing values are dropped, and any score/index column (e.g. \code{ideology_score})
is treated as metadata, not an answer. This is the shared field-extraction
step; how the result becomes prompt text is up to the caller.
}
\examples{
data(anes_2024_personas, package = "LLMR")
parts <- llm_persona_split(anes_2024_personas, 1)
names(parts$demographics)[1:3]
}
\seealso{
\code{\link[=llm_persona_demographic_fields]{llm_persona_demographic_fields()}}, \link{anes_2024_personas}.
}
