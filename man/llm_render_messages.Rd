% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview.R
\name{llm_render_messages}
\alias{llm_render_messages}
\title{Render tidy messages without calling any API}
\usage{
llm_render_messages(
  .data,
  prompt = NULL,
  .messages = NULL,
  .system_prompt = NULL,
  rows = NULL
)
}
\arguments{
\item{.data}{A data.frame/tibble whose columns feed the \code{glue} templates.}

\item{prompt}{A single \code{glue} template string (mutually exclusive with
\code{.messages}).}

\item{.messages}{A character vector of \code{glue} templates, optionally named by
role (\code{"system"}, \code{"user"}, \code{"assistant"}, \code{"file"}). Unnamed entries
default to \code{"user"}.}

\item{.system_prompt}{Optional system string, prepended when a row has no
\code{"system"} message.}

\item{rows}{Optional integer vector selecting which rows to render (default:
all rows).}
}
\value{
A list of length \code{length(rows)} (default \code{nrow(.data)}). Each element
is either a bare character scalar (prompt only, no system) or a role-named
character vector, identical to what the call path would dispatch.
}
\description{
Returns the per-row message objects that \code{\link[=llm_fn]{llm_fn()}} / \code{\link[=llm_mutate]{llm_mutate()}} would
build from \code{prompt} or \code{.messages}, using the same internal renderer they
use. No request is sent and no file is read or encoded; a \code{"file"} role
stays a (glued) path string. Use this to inspect templating, roles, and
multimodal wiring before spending anything.
}
\examples{
df <- data.frame(text = c("good", "bad"), stringsAsFactors = FALSE)
llm_render_messages(df, prompt = "Sentiment of: {text}")
llm_render_messages(
  df,
  .messages = c(system = "Be terse.", user = "Rate: {text}")
)
}
\seealso{
\code{\link[=llm_preview]{llm_preview()}} for a row-level summary with issue flags and the
batch plan; \code{\link[=llm_fn]{llm_fn()}}, \code{\link[=llm_mutate]{llm_mutate()}}.
}
