% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcript_messages.R
\name{transcript_as_messages}
\alias{transcript_as_messages}
\title{Build a role-flipped message array from a multi-speaker transcript}
\usage{
transcript_as_messages(
  transcript,
  speaker,
  system = NULL,
  instruction = NULL,
  label = function(id) paste0(id, ": "),
  sanitize = TRUE
)
}
\arguments{
\item{transcript}{A data.frame/tibble with character columns \code{speaker} and
\code{text}, in chronological order.}

\item{speaker}{The current speaker's id (matched against \code{transcript$speaker}).
Rows with this speaker become \code{assistant} turns; all others become labeled
\code{user} turns.}

\item{system}{Optional system text (persona, standing instructions) placed as
the leading \code{system} message. \code{NULL} to omit.}

\item{instruction}{Optional trailing instruction (current question, "your
turn" cue) placed as the final \code{user} message. \code{NULL} to omit.}

\item{label}{A function \code{(speaker_id) -> prefix} producing the in-content
label for other speakers' turns; the default renders \code{"<id>: "}. The
speaker's own turns are never labeled.}

\item{sanitize}{If \code{TRUE} (default), strip forged role headers and chat
control tokens from other speakers' text before embedding.}
}
\value{
A list of message objects (\code{list(role=, content=)}) suitable for
\code{\link[=call_llm]{call_llm()}}, guaranteed alternating and user-leading after any system.
}
\description{
Renders a shared, speaker-attributed transcript into a message array from
one speaker's point of view, the way a stateful chat is post-trained to read
it: the current speaker's own past turns become \code{assistant} messages (their
own voice), and every other speaker's turns become \code{user} messages labeled
with the speaker's name. An optional \code{system} block (persona, instructions)
leads the array, and an optional \code{instruction} (the current question or "your
turn" cue) closes it as a trailing \code{user} message. The result is passed
through \code{\link[=ensure_alternating_messages]{ensure_alternating_messages()}} so it is provider-safe.
}
\details{
Putting the speaker's own turns in the \code{assistant} role gives the model a
structural handle on "what I already said", which reduces self-repetition in
multi-agent conversations relative to flattening the whole transcript into one
\code{user} block. Other speakers' text stays in \code{user} turns (never \code{assistant}),
which preserves the trust boundary and is sanitized against forged role
headers.
}
\examples{
tx <- data.frame(
  speaker = c("Ana", "Ben", "Cy"),
  text    = c("I think we ban it.", "Too costly for renters.", "Phase it in."),
  stringsAsFactors = FALSE
)
# From Ben's perspective: his line is assistant, Ana's and Cy's are user.
transcript_as_messages(tx, speaker = "Ben",
                       system = "You are Ben, a renter.",
                       instruction = "Your turn, Ben.")
}
\seealso{
\code{\link[=ensure_alternating_messages]{ensure_alternating_messages()}}, \code{\link[=call_llm]{call_llm()}}
}
