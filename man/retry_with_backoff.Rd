% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LLM_robust_utils.R
\name{retry_with_backoff}
\alias{retry_with_backoff}
\title{Retry with Exponential Backoff}
\usage{
retry_with_backoff(
  func,
  tries = 3,
  initial_wait = 10,
  backoff_factor = 10,
  ...
)
}
\arguments{
\item{func}{A function to be invoked.}

\item{tries}{Integer. Number of attempts before giving up. Default 3.}

\item{initial_wait}{Numeric. Initial wait time (seconds). Default 10.}

\item{backoff_factor}{Numeric. Multiplier for wait time after each failure. Default 10.}

\item{...}{Arguments passed on to \code{func}.}
}
\value{
The result of \code{func(...)} if successful, or an error after all attempts fail.
}
\description{
A generic utility that calls a function repeatedly with exponential
increases in the wait time after each failure.
}
\examples{
\dontrun{
  # Illustrative usage:
  safe_func <- function(x) {
    if (runif(1) < 0.7) stop("Simulated failure.")
    paste("Success with x =", x)
  }
  result <- retry_with_backoff(safe_func, tries = 3, initial_wait = 5, backoff_factor = 2, x = 10)
  cat("Result:", result, "\n")
}
}
