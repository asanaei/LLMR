% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_read.R
\name{llm_request_from_log}
\alias{llm_request_from_log}
\title{Rebuild a callable request from a logged record}
\usage{
llm_request_from_log(record, on_unsupported = c("error", "warn", "quiet"))
}
\arguments{
\item{record}{A parsed log record: a list with at least \code{provider}, \code{model},
and \code{request} (as found in \code{llm_log_read(x)$records[[i]]$rec}).}

\item{on_unsupported}{What to do when the request body uses a shape this
reconstructor does not cover (so the rebuilt messages would be empty or
lossy) -- chiefly the OpenAI Responses API (\code{input}/\code{instructions}) and
non-text content blocks (images, files). \code{"error"} (default) aborts;
\code{"warn"} warns and returns the best-effort result; \code{"quiet"} returns it
silently. Failing loudly is the default because a silently empty replay is
worse than none.}
}
\value{
A list with \code{config} (an \code{\link[=llm_config]{llm_config()}}), \code{messages} (a named
character vector of role/content turns), and \code{complete} (a logical: \code{TRUE}
when the body was fully reconstructed, \code{FALSE} when content was dropped).
\code{NULL} when the record carries no request body.
}
\description{
Turns one parsed log record (the \code{rec} element of a \code{\link[=llm_log_read]{llm_log_read()}} entry)
back into the \code{config} and \code{messages} an experiments row needs, so an archived
call can be re-issued live or matched against a config-driven call. The
provider-specific request body is canonicalized to provider-neutral
role/content turns and its generation parameters are recovered onto a
reconstructed \code{\link[=llm_config]{llm_config()}}.
}
\examples{
rec <- list(provider = "openai", model = "gpt-4o-mini",
            request = list(
              messages = list(list(role = "user", content = "Hi")),
              temperature = 0))
req <- llm_request_from_log(rec)
req$messages
}
\seealso{
\code{\link[=llm_log_read]{llm_log_read()}}, \code{\link[=llm_request_hash]{llm_request_hash()}}
}
