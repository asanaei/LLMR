% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_log.R
\name{llm_log_enable}
\alias{llm_log_enable}
\alias{llm_log_disable}
\alias{llm_log_status}
\title{Record every LLM call in a local audit log}
\usage{
llm_log_enable(path = "llmr_log.jsonl", include_messages = TRUE)

llm_log_disable()

llm_log_status()
}
\arguments{
\item{path}{File path for the log. Created on first write; appended to if
it exists, so one file can accumulate a whole project's calls.}

\item{include_messages}{Logical. If \code{TRUE} (default), the request body and
the reply text are stored. If \code{FALSE}, only metadata is stored.}
}
\value{
\code{llm_log_enable()} and \code{llm_log_disable()} return the previous log
path invisibly. \code{llm_log_status()} returns the active path or \code{NULL},
invisibly, after printing a one-line status.
}
\description{
\code{llm_log_enable()} turns on a session-wide audit log: each API call made
through LLMR (including those issued by \code{\link[=llm_fn]{llm_fn()}}, \code{\link[=llm_mutate]{llm_mutate()}},
\code{\link[=call_llm_par]{call_llm_par()}}, and \code{\link[=chat_session]{chat_session()}}) appends one JSON object to \code{path}.
\code{llm_log_disable()} turns logging off. \code{llm_log_status()} reports the
current destination, if any.
}
\details{
Methodological guidance for LLM-assisted research asks authors to retain,
for every call: the model and provider, the full prompt, the inference
settings, the output, and identifiers that allow an exact lookup later.
The audit log records precisely that:
\itemize{
\item \code{ts}: ISO-8601 timestamp with timezone.
\item \code{provider}, \code{model}: as configured; \code{model_version}: the identifier the
server reports having served (when echoed), which catches silent model
updates.
\item \code{request}: the JSON body sent to the provider, including all sampling
parameters and the rendered messages. Inline file data (base64) is
replaced by a short placeholder so logs stay small.
\item \code{text}, \code{finish_reason}, \code{usage}: the reply, why it stopped, and token
counts (including cached tokens when reported).
\item \code{response_id}, \code{status}, \code{duration_s}: provider request id, HTTP status,
and wall-clock seconds.
\item Failed calls are logged too (\code{kind = "error"}), with the provider's error
message.
}

Records are appended line by line; under parallel execution all workers
append to the same file. Each line is one complete record, so interleaving
across workers is harmless. The log contains your prompts and the model's
replies in clear text. It never contains API keys.

Set \code{include_messages = FALSE} to omit request bodies and reply text
(keeping only metadata, parameters, usage, and identifiers), e.g. when
prompts contain confidential data.
}
\examples{
\dontrun{
llm_log_enable("annotation_run.jsonl")
cfg <- llm_config("groq", "openai/gpt-oss-20b")
call_llm(cfg, "One word: capital of France?")
llm_log_disable()

# Read the log back as a data frame
log_df <- jsonlite::stream_in(file("annotation_run.jsonl"), verbose = FALSE)
}
}
\seealso{
\code{\link[=llm_usage]{llm_usage()}} for token summaries, \code{\link[=llm_methods_text]{llm_methods_text()}} for a
draft methods paragraph.
}
