% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/secrets.R
\name{llm_api_key_env}
\alias{llm_api_key_env}
\title{Declare an API key sourced from an environment variable}
\usage{
llm_api_key_env(var, required = TRUE, default = NULL)
}
\arguments{
\item{var}{Name of the environment variable (e.g., "OPENAI_API_KEY").}

\item{required}{If TRUE, a missing variable raises an authentication error at
call time.}

\item{default}{Optional default used if the environment variable is not set.}
}
\value{
A secret handle to pass as \code{api_key = llm_api_key_env("VARNAME")} in
\code{\link[=llm_config]{llm_config()}}.
}
\description{
Reference an API key by the name of the environment variable that holds it,
so the secret never appears in your R code or saved objects. Store the key in
your shell profile or in \verb{~/.Renviron} (e.g. \code{OPENAI_API_KEY=sk-...}).
}
\details{
Best practice is to not pass a key explicitly at all: \code{\link[=llm_config]{llm_config()}} already
looks up the standard variable for each provider (\verb{<PROVIDER>_API_KEY}, then
\verb{<PROVIDER>_KEY}). Use \code{llm_api_key_env()} only when your variable has a
non-standard name.
}
\examples{
cfg <- llm_config(
  "openai", "gpt-4o-mini",
  api_key = llm_api_key_env("MY_OPENAI_KEY")
)
}
\seealso{
\code{\link[=llm_config]{llm_config()}}
}
