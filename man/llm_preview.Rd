% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview.R
\name{llm_preview}
\alias{llm_preview}
\title{Preview a tidy LLM call without spending anything}
\usage{
llm_preview(
  .data,
  prompt = NULL,
  .messages = NULL,
  .system_prompt = NULL,
  .config = NULL,
  .structured = FALSE,
  .schema = NULL,
  .tags = NULL,
  .return = c("columns", "text", "object"),
  .batch_size = 1L,
  rows = NULL,
  max_chars = 500L
)
}
\arguments{
\item{.data}{A data.frame/tibble whose columns feed the \code{glue} templates.}

\item{prompt}{A single \code{glue} template string (mutually exclusive with
\code{.messages}).}

\item{.messages}{A character vector of \code{glue} templates, optionally named by
role (\code{"system"}, \code{"user"}, \code{"assistant"}, \code{"file"}). Unnamed entries
default to \code{"user"}.}

\item{.system_prompt}{Optional system string, prepended when a row has no
\code{"system"} message.}

\item{.config}{Optional \code{\link[=llm_config]{llm_config()}}. When supplied, preview checks the
embedding-vs-row-batching conflict; otherwise config-dependent checks are
skipped.}

\item{.structured}{Logical; if \code{TRUE}, the call would request structured
JSON output. Used only to validate \code{.schema}/\code{.tags} combinations.}

\item{.schema}{Optional JSON schema (list). Flagged if supplied without
\code{.structured = TRUE}.}

\item{.tags}{Optional character vector of tag names. Flagged if combined with
\code{.structured = TRUE} (structured takes precedence).}

\item{.return}{One of \code{"columns"}, \code{"text"}, \code{"object"}. Only used to flag
the unsupported \code{"object"} + batching combination.}

\item{.batch_size}{Rows per call. \code{1} (default) means one call per row;
\verb{> 1} or \code{Inf} packs rows into batched calls.}

\item{rows}{Optional integer vector selecting which rows to render (default:
all rows).}

\item{max_chars}{Truncate each row's rendered preview to this many characters
(default 500). Set higher to see full prompts.}
}
\value{
A tibble of class \code{llmr_preview}, one row per previewed input row,
with columns: \code{row}, \code{ok} (no issues), \code{roles}, \code{rendered_preview},
\code{chars}, \code{has_file}, \code{file_ok}, \code{batch_id}, \code{batch_size}, \code{batch_row}, and
\code{issues} (a list-column of character vectors).
}
\description{
Renders every row exactly as \code{\link[=llm_fn]{llm_fn()}} / \code{\link[=llm_mutate]{llm_mutate()}} would (no API call,
no file I/O), then reports a tidy, row-level summary: the rendered text, the
roles, character counts, file presence and existence, the batch plan, and a
list-column of \code{issues}. Problems that would only surface mid-run (a missing
file, a \code{"file"} role combined with \code{.batch_size > 1}, an embedding config
with row batching, \code{.return = "object"} with batching, a schema supplied
without \code{.structured}) are collected per row so you see all of them at once
rather than hitting the first error.
}
\details{
Batched data travels inside numbered \verb{<row_i>...</row_i>} tags; the
\code{batch_id} / \code{batch_size} / \code{batch_row} columns show how rows would be
grouped into calls at the given \code{.batch_size}.
}
\examples{
df <- data.frame(text = c("a", "b", "c"), stringsAsFactors = FALSE)
llm_preview(df, prompt = "Classify: {text}", .batch_size = 2)
}
\seealso{
\code{\link[=llm_render_messages]{llm_render_messages()}}, \code{\link[=llm_usage]{llm_usage()}}, \code{\link[=llm_failures]{llm_failures()}}.
}
