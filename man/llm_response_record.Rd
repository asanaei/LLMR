% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/response_record.R
\name{llm_response_record}
\alias{llm_response_record}
\title{Flatten one LLM response to a provenance row}
\usage{
llm_response_record(
  x,
  request = NULL,
  config = NULL,
  error = NULL,
  started_at = NULL,
  ended_at = NULL
)
}
\arguments{
\item{x}{An \link{llmr_response}. If \code{x} is a condition (a caught error) it is
treated as a failed call. Any other object yields a failed row whose
message notes the unexpected class.}

\item{request}{The messages or prompt that produced \code{x}, used together with
\code{config} to fill \code{request_hash} via \code{\link[=llm_request_hash]{llm_request_hash()}}. \code{NULL} leaves
\code{request_hash} as \code{NA}.}

\item{config}{The \code{\link[=llm_config]{llm_config()}} used for the call, for \code{request_hash} and to
backfill \code{provider}/\code{model} when the response does not carry them.}

\item{error}{Optional caught condition to record instead of (or alongside) a
missing response; when supplied, the row is a failure carrying its message.}

\item{started_at, ended_at}{Optional \code{POSIXct}/numeric timestamps. When both
are given and the response does not report a duration, \code{duration_s} is their
difference; \code{created_at} is \code{ended_at} when supplied.}
}
\value{
A one-row tibble with the response-contract columns.
}
\description{
Turns a single \link{llmr_response} (or a caught error) into a one-row tibble with
the columns the LLMR family treats as the response contract:
\code{response_text}, \code{response_id}, \code{provider}, \code{model}, \code{model_version},
\code{finish_reason}, \code{sent_tokens}, \code{rec_tokens}, \code{total_tokens},
\code{reasoning_tokens}, \code{cached_tokens}, \code{success}, \code{error_message},
\code{duration_s}, \code{created_at}, and \code{request_hash}. It is the canonical flattening
path: reuse it rather than reaching into a response's fields, so a sealed
archive or an audit reconstructs the same columns everywhere.
}
\details{
A failure is a record, not a gap. Pass a caught condition (or any non-response
object) as \code{error}, or as \code{x}, and the row carries \code{success = FALSE} with the
message in \code{error_message}; the call is never silently dropped.
}
\examples{
r <- structure(
  list(text = "Hello!", provider = "openai", model = "demo",
       model_version = NA_character_, finish_reason = "stop",
       usage = list(sent = 12L, rec = 5L, total = 17L,
                    reasoning = NA_integer_, cached = NA_integer_),
       response_id = "resp_123", duration_s = 0.012),
  class = "llmr_response")
llm_response_record(r)
# a caught error is a row too:
llm_response_record(simpleError("boom"))
}
\seealso{
\code{\link[=llm_request_hash]{llm_request_hash()}}, \link{llmr_response}, \code{\link[=llm_usage]{llm_usage()}}
}
