% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LLM_parallel_utils.R
\name{llm_add_request_hash}
\alias{llm_add_request_hash}
\title{Append the audit-log request hash to a parallel-results frame}
\usage{
llm_add_request_hash(df)
}
\arguments{
\item{df}{A data frame with \code{config} and \code{messages} columns.}
}
\value{
\code{df} with an added \code{request_hash} character column.
}
\description{
Adds a \code{request_hash} column to a data frame that still carries the \code{config}
and \code{messages} list-columns (such as the result of \code{\link[=call_llm_par]{call_llm_par()}}). The
hash is the same key \code{\link[=llm_request_hash]{llm_request_hash()}} produces, so the parallel path can
be joined to the audit log written by \code{\link[=llm_log_enable]{llm_log_enable()}}. Equivalent to
calling \code{call_llm_par(..., .request_hash = TRUE)} after the fact.
}
\seealso{
\code{\link[=call_llm_par]{call_llm_par()}}, \code{\link[=llm_request_hash]{llm_request_hash()}}.
}
