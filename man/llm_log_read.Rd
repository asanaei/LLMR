% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_read.R
\name{llm_log_read}
\alias{llm_log_read}
\title{Read an LLMR audit log into records and a manifest}
\usage{
llm_log_read(log)
}
\arguments{
\item{log}{Path to a JSONL audit log.}
}
\value{
A list with \code{records} (a list of \code{list(raw=, rec=)}, one per line) and
\code{manifest} (a tibble with \code{idx}, \code{ts}, \code{kind}, \code{provider}, \code{model},
\code{model_version}, \code{status}, \code{schema_version}, \code{has_payload}, \code{request_hash},
\code{record_hash}).
}
\description{
Parses a JSONL audit log written by \code{\link[=llm_log_enable]{llm_log_enable()}} into its records and a
per-record manifest tibble. Each manifest row carries the call's metadata, a
\code{record_hash} over the verbatim log line (tamper evidence), and, where the
request body was logged, a \code{request_hash} identifying the call. The
\code{request_hash} is computed through \code{\link[=llm_request_hash]{llm_request_hash()}} over the call's
canonical turns and generation parameters, so a call read from a log and the
same call described by a config hash identically.
}
\examples{
# In practice the log comes from llm_log_enable("study.jsonl"); here one
# record written by hand.
log <- tempfile(fileext = ".jsonl")
writeLines(paste0(
  '{"ts":"2026-06-01T10:00:01+0000","schema_version":"1.0","kind":"call",',
  '"provider":"openai","model":"gpt-4o-mini","status":200,',
  '"request":{"messages":[{"role":"user","content":"Capital of France?"}],',
  '"temperature":0},"usage":{"sent":5,"rec":1},',
  '"response_id":"r-1","text":"Paris"}'), log)
read <- llm_log_read(log)
read$manifest
}
\seealso{
\code{\link[=llm_request_hash]{llm_request_hash()}}, \code{\link[=llm_log_enable]{llm_log_enable()}}
}
