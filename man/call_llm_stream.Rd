% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream.R
\name{call_llm_stream}
\alias{call_llm_stream}
\title{Stream a chat completion token by token}
\usage{
call_llm_stream(
  config,
  messages,
  callback = function(chunk) cat(chunk),
  verbose = FALSE
)
}
\arguments{
\item{config}{An \link{llm_config} for a generative model.}

\item{messages}{Messages as in \code{\link[=call_llm]{call_llm()}}.}

\item{callback}{Function called with each text chunk (a character scalar)
as it arrives. The default prints chunks to the console with \code{cat()}.
Reasoning deltas (when a provider streams them separately) are not passed
to \code{callback}; they are collected into the result's \code{thinking} field.}

\item{verbose}{Print the assembled response object at the end.}
}
\value{
An \link{llmr_response} assembled from the stream (invisibly). Token
usage is filled when the provider reports it in the stream; otherwise it
is \code{NA}.
}
\description{
Like \code{\link[=call_llm]{call_llm()}}, but the reply arrives incrementally: \code{callback} is
invoked with each text chunk as it is generated, and the complete
\link{llmr_response} is returned at the end. Streaming keeps long generations
responsive and avoids HTTP timeouts on slow, lengthy completions.
}
\details{
Supported providers: all OpenAI-compatible chat APIs (openai, groq,
together, deepseek, xai, alibaba, zhipu, moonshot, xiaomi, ollama),
Anthropic, and Gemini. The request body is built by the same internals as
\code{\link[=call_llm]{call_llm()}}, so parameters, structured output, and hooks behave
identically; only the transport differs.
}
\examples{
\dontrun{
cfg <- llm_config("groq", "openai/gpt-oss-20b")
r <- call_llm_stream(cfg, "Tell a 100-word story about a lighthouse.")
tokens(r)
}
}
\seealso{
\code{\link[=call_llm]{call_llm()}}, \code{\link[=chat_session]{chat_session()}}
}
