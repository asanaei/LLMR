% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R, R/usage.R
\name{report}
\alias{report}
\alias{report.llmr_experiment}
\title{Draft a methods-section report from an LLMR-family result object}
\usage{
report(x, ...)

\method{report}{llmr_experiment}(x, prefix = NULL, task = NULL, ...)
}
\arguments{
\item{x}{An LLMR experiment or an object returned by an LLMR method package.}

\item{...}{Passed to methods (some methods require extra arguments, e.g.
some LLMRcontent report methods require the gold set and protocol).}

\item{prefix}{For an \code{llmr_experiment}, the output-column prefix when the
object uses \code{\link[=llm_mutate]{llm_mutate()}} diagnostic names. It is inferred when possible.}

\item{task}{For an \code{llmr_experiment}, an optional clause describing the task,
such as \code{"to classify open-ended responses"}.}
}
\value{
A method-defined report object, by convention a character vector
with a print method. The \code{llmr_experiment} method returns a character
scalar containing a draft methods paragraph.
}
\description{
A shared generic across the LLMR method packages. It returns the
methods-section prose and tables for a result object (what a paper's
appendix would print), as distinct from \code{\link[=diagnostics]{diagnostics()}}, which returns the
machine-readable numbers.
}
\details{
LLMR implements this generic for \code{llmr_experiment} objects returned by
\code{\link[=call_llm_par]{call_llm_par()}} and its wrappers. The method packages (LLMRcontent,
LLMRpanel) provide methods for their own result classes.
}
\examples{
\dontrun{
results <- call_llm_broadcast(cfg, c("First prompt", "Second prompt"))
cat(report(results, task = "to classify short texts"))
}
}
\seealso{
\code{\link[=diagnostics]{diagnostics()}}, \code{\link[=llm_usage]{llm_usage()}}, \code{\link[=llm_log_enable]{llm_log_enable()}}
}
