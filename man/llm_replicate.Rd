% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replicate.R
\name{llm_replicate}
\alias{llm_replicate}
\title{Run the same prompt several times per row}
\usage{
llm_replicate(
  .data,
  output,
  prompt,
  .config,
  .times = 3L,
  .system_prompt = NULL,
  ...
)
}
\arguments{
\item{.data}{A data.frame / tibble.}

\item{output}{Unquoted base name for the replicate columns.}

\item{prompt}{A glue template string evaluated against the columns of
\code{.data} (as in \code{\link[=llm_mutate]{llm_mutate()}}).}

\item{.config}{An \link{llm_config} object (generative).}

\item{.times}{Number of replicates (default 3).}

\item{.system_prompt}{Optional system message.}

\item{...}{Passed to \code{\link[=call_llm_par]{call_llm_par()}} (e.g. \code{tries}, \code{progress}).}
}
\value{
\code{.data} with \code{.times} new character columns,
\verb{<output>_1 ... <output>_<.times>} (\code{NA} where a call failed).
}
\description{
Calls the model \code{.times} times for every row of \code{.data} (all replicates run
through the parallel engine in one pass) and appends one column per
replicate: \verb{<output>_1}, \verb{<output>_2}, .... Feed the result to
\code{\link[=llm_agreement]{llm_agreement()}} for per-row majority labels and overall reliability.
}
\details{
Replication only measures sampling variability if the model can vary: with
\code{temperature = 0} (or a fixed \code{seed}) most providers return nearly identical
draws, which inflates agreement. Conversely, for measurement purposes you
may want exactly that check: high disagreement at low temperature signals a
prompt the model finds genuinely ambiguous.
}
\examples{
\dontrun{
cfg <- llm_config("groq", "openai/gpt-oss-20b", temperature = 1)
df <- tibble::tibble(text = c("I loved it", "Meh", "Terrible service"))
reps <- df |>
  llm_replicate(sentiment,
                prompt = "Sentiment of '{text}'. One word: positive, negative, or neutral.",
                .config = cfg, .times = 5)
llm_agreement(reps, prefix = "sentiment")
}
}
\seealso{
\code{\link[=llm_agreement]{llm_agreement()}}, \code{\link[=llm_mutate]{llm_mutate()}}, \code{\link[=call_llm_par]{call_llm_par()}}
}
