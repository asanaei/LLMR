% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_mode.R
\name{llm_parse_rowpack_tags}
\alias{llm_parse_rowpack_tags}
\title{Parse a batched, row-wrapped tag response into per-row field lists}
\usage{
llm_parse_rowpack_tags(text, tags, m)
}
\arguments{
\item{text}{Character scalar: one batched model response containing
\verb{<row_i>...</row_i>} blocks, each wrapping flat field tags.}

\item{tags}{Character vector of field tag names to extract within each block.}

\item{m}{Integer: the number of items expected in the batch (local ids
\verb{1..m}).}
}
\value{
A list of length \code{m}. Element \code{i} is the named list returned by
\code{\link[=llm_parse_tags]{llm_parse_tags()}} for \verb{<row_i>}, or \code{NULL} when that block is absent,
truncated, or otherwise unrecoverable.
}
\description{
Splits a single batched reply into its numbered \verb{<row_i>} blocks and then
applies the standard flat tag parser (\code{\link[=llm_parse_tags]{llm_parse_tags()}}) inside each block.
This is the parsing counterpart to the \verb{<row_i>} protocol that LLMR uses when
\code{.rows_per_prompt > 1} together with \code{.tags}; it is exported so the protocol is
inspectable and testable on its own.
}
\details{
Robustness mirrors the internal scanner: reordered, duplicated, hallucinated,
truncated, or accidentally nested \verb{<row_i>} blocks are handled; only fully
closed blocks contribute. Inner field tags are extracted by the same parser
used in non-batched tag mode, so values coerce and decode identically.
}
\examples{
txt <- paste(
  "<row_1><age>21</age><job>barista</job></row_1>",
  "<row_2><age>34</age><job>welder</job></row_2>",
  sep = "\n"
)
llm_parse_rowpack_tags(txt, tags = c("age", "job"), m = 2)
}
\seealso{
\code{\link[=llm_parse_tags]{llm_parse_tags()}}, \code{\link[=llm_parse_tags_col]{llm_parse_tags_col()}}, \code{\link[=llm_mutate]{llm_mutate()}},
\code{\link[=llm_fn]{llm_fn()}}
}
