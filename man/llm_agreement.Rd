% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replicate.R
\name{llm_agreement}
\alias{llm_agreement}
\title{Agreement across replicated LLM annotations}
\usage{
llm_agreement(
  .data,
  cols = NULL,
  prefix = NULL,
  normalize = TRUE,
  metric = c("nominal", "ordinal", "interval")
)
}
\arguments{
\item{.data}{A data frame holding the replicate columns.}

\item{cols}{Character vector naming the replicate columns. Alternatively
supply \code{prefix}.}

\item{prefix}{Base name: columns matching \verb{<prefix>_1}, \verb{<prefix>_2}, ...
are used.}

\item{normalize}{If \code{TRUE} (default), values are compared after trimming
whitespace and lowercasing, so "Positive" and " positive" agree. Set to
\code{FALSE} for exact string comparison.}

\item{metric}{Difference function for Krippendorff's alpha: \code{"nominal"}
(default, categories either match or do not), \code{"ordinal"} (ordered
categories), or \code{"interval"} (numeric distance). For \code{"ordinal"} and
\code{"interval"}, labels that parse as numbers are ordered numerically (so "2"
precedes "10"); \code{"interval"} requires numeric labels and returns \code{NA} with a
warning otherwise. The default reproduces the previous nominal-only behavior
exactly.}
}
\value{
An object of class \code{llmr_agreement}: a list with
\describe{
\item{\code{by_row}}{a tibble with one row per unit: \code{majority} (modal
label, \code{NA} on ties), \code{share} (modal share of non-missing
replicates), \code{n_distinct}, \code{unanimous}, \code{tie}, \code{n_missing}.}
\item{\code{summary}}{a one-row tibble: \code{n_units}, \code{n_replicates},
\code{mean_pairwise_agreement}, \code{krippendorff_alpha}, \code{n_unanimous},
\code{n_ties}.}
}
Printing shows the summary.
}
\description{
Computes per-row majority labels and overall reliability for replicate
columns produced by \code{\link[=llm_replicate]{llm_replicate()}} (or any set of columns holding
repeated codings of the same units, including codings by different models
or by humans). Reliability is reported as average pairwise percent
agreement and Krippendorff's alpha for nominal data, the statistic
reviewers most often ask for; alpha handles missing values (failed calls)
gracefully.
}
\references{
Krippendorff, K. (2019). Content Analysis: An Introduction to
Its Methodology (4th ed.), chapter 12. The alpha implemented here is the
nominal-data form with missing values allowed.
}
\seealso{
\code{\link[=llm_replicate]{llm_replicate()}}
}
