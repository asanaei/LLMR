% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LLM_parallel_utils.R
\name{llm_cross_design}
\alias{llm_cross_design}
\title{Cross a data frame with LLM configs}
\usage{
llm_cross_design(
  .data,
  configs,
  prompt = NULL,
  .messages = NULL,
  .system_prompt = NULL
)
}
\arguments{
\item{.data}{A data frame containing variables for the glue prompt.}

\item{configs}{A list of \link{llm_config} objects (or a single \link{llm_config}).}

\item{prompt}{A glue string for a single user turn.}

\item{.messages}{Optional named character vector of glue templates (roles as names).}

\item{.system_prompt}{Optional system prompt template (glue string).}
}
\value{
A tibble with all original data columns plus \code{config} and \code{messages}
list-columns.
}
\description{
Creates an experimental design tibble that crosses every row in \code{.data} with
every config in \code{configs}, evaluating glue prompt templates row-by-row.
The result has \code{config} and \code{messages} list-columns ready for \code{\link[=call_llm_par]{call_llm_par()}}.
}
\examples{
\dontrun{
cities <- data.frame(city = c("Cairo", "Lima"))
cfgs <- list(llm_config("openai", "gpt-4.1-nano"), llm_config("openai", "gpt-4.1-mini"))
design <- llm_cross_design(cities, cfgs, prompt = "What country is {city} in?")
results <- call_llm_par(design)
}

}
\seealso{
\code{\link[=expand_llm_config]{expand_llm_config()}}, \code{\link[=call_llm_par]{call_llm_par()}}, \code{\link[=build_factorial_experiments]{build_factorial_experiments()}}
}
