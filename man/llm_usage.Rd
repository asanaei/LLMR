% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usage.R
\name{llm_usage}
\alias{llm_usage}
\title{Summarize token usage and outcomes of an LLM run}
\usage{
llm_usage(x, prefix = NULL)
}
\arguments{
\item{x}{A data frame from \code{\link[=call_llm_par]{call_llm_par()}} or \code{\link[=llm_mutate]{llm_mutate()}}.}

\item{prefix}{For an \code{\link[=llm_mutate]{llm_mutate()}} result, the output column name whose
diagnostics to summarize (e.g. \code{"answer"}). Inferred automatically when a
single diagnostic block is present; required when several are.}
}
\value{
A one-row tibble: \code{n}, \code{n_ok}, \code{n_failed}, \code{ok_rate}, \code{n_truncated}
(finish \code{"length"}), \code{n_filtered} (finish \code{"filter"}), \code{sent_tokens},
\code{rec_tokens}, \code{total_tokens}, \code{reasoning_tokens}, \code{duration_s}, and (when a
batch id column is present) \code{batch_calls} and \code{rows_per_batch_call}.
}
\description{
Reads the diagnostic columns produced by \code{\link[=call_llm_par]{call_llm_par()}} (and
\code{\link[=call_llm_broadcast]{call_llm_broadcast()}} / \code{\link[=llm_fn]{llm_fn()}} with \code{.return = "columns"}) or by
\code{\link[=llm_mutate]{llm_mutate()}}, and returns a one-row tibble of counts and token totals. It
reports \strong{tokens, not money}: sent, received, total, and reasoning tokens
are summed with \code{na.rm = TRUE} (correct under row batching, which attributes a
batch's tokens to its first row and leaves the rest \code{NA}). To estimate cost,
multiply these by your provider's current per-token prices yourself.
}
\examples{
res <- tibble::tibble(
  success = c(TRUE, TRUE, FALSE),
  finish_reason = c("stop", "length", "error:rate_limit"),
  sent_tokens = c(10L, 12L, NA_integer_),
  rec_tokens = c(5L, 7L, NA_integer_),
  total_tokens = c(15L, 19L, NA_integer_),
  reasoning_tokens = c(NA_integer_, NA_integer_, NA_integer_),
  duration = c(0.4, 0.5, 0.1)
)
llm_usage(res)
}
\seealso{
\code{\link[=llm_failures]{llm_failures()}}, \code{\link[=llm_preview]{llm_preview()}}, \code{\link[=llm_par_resume]{llm_par_resume()}}.
}
